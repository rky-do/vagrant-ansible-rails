# Vagrant Rails

## Stack

+ Rbenv
+ Rails
+ Node.js

## How to use

```sh
$ brew install ansible
$ git clone git@github.com:ryog/vagrant-ansible-rails.git
$ cd vagrant-ansible-rails
$ vagrant up
```
